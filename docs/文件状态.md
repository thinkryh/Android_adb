# 源码与发布文件状态

核对日期：2026-09-25。本页说明哪些内容进入 Git、哪些仅存在于本机构建目录。发布文件路径与大小是本机快照，后续构建或清理后需重新核对。

## Git 与发布文件

- 仓库目录：`Android投屏助手_v1/`。v2 的 WPF 项目、`Assets/` 图标、`MediaStorage.cs`、`ProjectionCaptureService.cs`、`ThemeManager.cs` 和 `docs/` 都属于应随源码提交的内容。验证仓库是否已同步时，以实际 `git status`、远端分支和 GitHub 页面为准。
- 本机 `Android投屏助手_v2/bin/Release/net10.0-windows/glass-v16/` 已有 `Android投屏助手.exe`、程序 DLL、QRCoder 和 `scrcpy/` 依赖，共 19 个文件、25.60 MiB。根 BAT 优先搜索这里；旧版 `glass-v15` 等仍在本机作为回退。
- `.gitignore` 忽略 `Android投屏助手_v2/bin/`、`obj/` 及本地无线地址记录。源码进入 Git 后，发布程序仍不会随普通提交自动出现。对外提供可直接运行的 v2 时，需另做完整发布包并验证目标电脑 Runtime。
- `scrcpy/` 的 13 个文件由 Git 跟踪，包括 `scrcpy.exe`、`adb.exe`、相关 DLL、server 与 `LICENSE.txt`。不要只复制主 EXE。

## 需要维护的入口和说明

| 路径 | 当前角色／状态 |
| --- | --- |
| `Android投屏助手.bat` | 推荐总入口；优先搜索 `glass-v16`，没有 v2 可执行文件时退回 v1 |
| `Android投屏助手_v2/Android投屏助手.csproj` | WPF 源码项目；构建时复制上级 `scrcpy/` |
| `Android投屏助手_v2/Android投屏助手_v2.bat` | 旧开发入口，只查 `publish`、`win-x64/publish` 和 Release 根目录；不作为本机最新版本入口 |
| `程序组件/Android投屏助手.ps1` | v1 保留脚本；无 v2 发布文件时由根 BAT 调用 |
| `README.md`、`Android投屏助手使用说明.txt` | 仓库首页和简明用户说明 |
| `Android投屏助手_v2/README.md`、`Android投屏助手_v2/实施状态.md` | v2 入口说明与实施状态摘要 |
| `docs/` | 功能目标、技术约束、验证失败记录与本页状态快照 |

## 本机运行数据

- 最近成功无线连接地址：`%LOCALAPPDATA%\Android投屏助手\settings.json`；主题：`%LOCALAPPDATA%\Android投屏助手\theme.txt`。这些文件与 Git 工作区分离。
- 截图与录屏：当前用户“图片\Android投屏助手”。Android 录屏未导出前暂存在手机 `/sdcard/`；故障时优先检查手机原文件。
- 当前文件清单不等于当前进程清单。排查运行问题时应核对进程实际路径，不能以目录存在推断最新 `glass-v16` 已被加载。

## 后续交接前检查

1. 查看 `git status` 与 `git diff`，确认主题、截图、图标与文档都已进入预期提交。
2. 重新发布到明确命名的目录，检查 `Android投屏助手.exe`、`QRCoder.dll`、`scrcpy/adb.exe`、`scrcpy/scrcpy.exe` 与依赖 DLL 齐全。
3. 在目标电脑验证 .NET 10 Desktop Runtime、USB 授权、无线连接、截图／录屏实际文件，再打包分发；发布包应保留第三方许可文件。
